# =====================
# Frontend - Variables de Entorno
# =====================
# Copia este archivo a .env

# URL de la API del backend
# En desarrollo local con Vite proxy: /api (o http://localhost:3000 directamente)
# En producción con Docker: /api (nginx hace el proxy)
VITE_API_URL=/api
